/* AIM */
// the concern is use BINARY SEARCH to return first and last array item using binary search

/* INITS */
let nums = [2,2,2,3,4,5];
let target = [2];


let lo = 0;
let numsSize = nums.length
let hi = numsSize -1;
let middle = Math.floor((lo+hi)/2);

let matchedPositions = new Array(2).fill(0);
let leftLimit;
let rightLimit;


/* WORK */
//1. while WHAT
//2. 
 
